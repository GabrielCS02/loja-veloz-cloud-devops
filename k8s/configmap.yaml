apiVersion: v1
kind: ConfigMap
metadata:
  name: pedidos-config
  namespace: loja-veloz
data:
  SPRING_PROFILES_ACTIVE: "docker"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "loja_veloz"