apiVersion: v1
kind: Service
metadata:
  name: pedidos-service
  namespace: loja-veloz
  labels:
    app: pedidos-service

spec:
  type: NodePort

  selector:
    app: pedidos-service

  ports:
    - name: http
      protocol: TCP
      port: 8082
      targetPort: http
      nodePort: 30082